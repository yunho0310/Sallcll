# 기여 가이드

## 브랜치 규칙

- `main`에서 직접 작업하지 않습니다.
- 작업 전 `develop`(또는 `main`)에서 새 브랜치를 생성합니다.
  - `feature/{기능명}`, `fix/{버그명}`, `chore/{작업명}` 형식을 사용합니다.

## 커밋 메시지 규칙

```
<type>: <설명>

예)
feat: 로그인 기능 추가
fix: 회원가입 유효성 검사 오류 수정
docs: README 업데이트
refactor: 코드 구조 개선
chore: 설정 파일 변경
```

## PR(Pull Request) 규칙

1. 작업이 끝나면 대상 브랜치로 PR을 생성합니다.
2. PR 제목과 설명에 변경 사항과 이유를 작성합니다.
3. 최소 1명 이상의 리뷰를 받은 후 병합합니다.
4. 병합 방식은 Squash and merge를 기본으로 합니다.

## 이슈

- 작업 전 관련 이슈를 생성하고, 진행 중인 작업은 이슈에 담당자를 지정합니다.
